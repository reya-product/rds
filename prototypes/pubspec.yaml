name: prototypes
description: Reya feature prototypes — clickthrough demos for customer sharing.
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  rds:
    path: ../packages/rds
  go_router: ^14.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
